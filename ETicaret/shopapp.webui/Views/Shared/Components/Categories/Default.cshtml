@model List<Category>

<div class="list-group">
    <a asp-controller="Shop" asp-action="List" asp-route-category="" class="list-group-item list-group-item-action">Tüm Kategoriler</a>
    @foreach (var item in Model)
    {

        <a 
        asp-controller="Shop"
        asp-action="List"
        asp-route-category="@item.Url"
        title="@(item.Name) Kategorisi"
        class="list-group-item list-group-item-action @(ViewBag.selectedCategory==item.Url?"active":"")"
        >
            @item.Name
        </a>
    }
</div>